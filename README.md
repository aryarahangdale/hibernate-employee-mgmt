# Hibernate Employee Management API

Spring Boot 3 + Hibernate (Spring Data JPA) + H2 in-memory database.

## Run

1. `mvn spring-boot:run`
2. H2 Console: http://localhost:8080/h2-console (JDBC: jdbc:h2:mem:testdb)
3. API: http://localhost:8080/api/employees
